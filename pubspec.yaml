name: miply
description: "Lokaler Musikplayer ohne Server."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  just_audio: ^0.9.40
  just_audio_windows: ^0.2.3
  just_audio_background: ^0.0.1-beta.17
  smtc_windows: ^1.1.0
  sqflite: ^2.3.0
  sqflite_common: ^2.5.9
  sqflite_common_ffi: ^2.4.1
  sqflite_common_ffi_web: ^1.1.2
  path: ^1.9.0
  path_provider: ^2.1.5
  permission_handler: ^12.0.0
  file_selector: ^1.1.0
  speech_to_text: ^7.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
